services:
  - type: web
    name: questionpapers
    env: dotnet
    region: oregon
    buildCommand: dotnet build -c Release
    startCommand: dotnet run --project . --urls "http://0.0.0.0:$PORT"
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production